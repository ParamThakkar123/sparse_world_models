# Pinned to the versions the reported results were produced with (single laptop,
# Intel Core i7-12650H CPU / NVIDIA RTX 3050 GPU, CUDA 12.6). Everything runs on
# CPU too -- pass `--device cpu` (the default) if you have no GPU.
torch==2.12.0
numpy==2.2.6
matplotlib==3.9.4
mujoco==3.5.0
thop==0.1.1.post2209072238
pytest==9.0.2
